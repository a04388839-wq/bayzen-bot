# 📤 Как загрузить бота на GitHub (пошаговая инструкция)

## 🎯 Шаг за шагом

### ШАГ 1: Откройте ваш новый репозиторий

1. Откройте https://github.com
2. Войдите в аккаунт
3. Найдите ваш новый репозиторий в списке
4. Откройте его (нажмите на название)

---

### ШАГ 2: Загрузите файлы

#### Вариант А: Загрузка через "Upload files" (РЕКОМЕНДУЕТСЯ)

1. На странице репозитория нажмите зеленую кнопку **"Add file"** (вверху справа)
2. Выберите **"Upload files"**

3. Откройте папку с ботом на вашем компьютере:
   - `C:\Users\bayas\OneDrive\Desktop\Bayzen Learn`

4. **Выберите ВСЕ файлы и папки:**
   - Зажмите `Ctrl` и кликайте на файлы, или
   - Выделите все файлы (`Ctrl+A`)
   - Перетащите их в окно браузера GitHub

   **Что загружать:**
   - ✅ `bot.py`
   - ✅ `requirements.txt`
   - ✅ `README.md`
   - ✅ `Dockerfile` (если есть)
   - ✅ `bothost.json` (если есть)
   - ✅ `.dockerignore` (если есть)
   - ✅ Папку `images/` (со всеми фото внутри)
   - ❌ НЕ загружайте `bot_database.db` (база данных)
   - ❌ НЕ загружайте `start_bot.bat` (можно, но не обязательно)
   - ❌ НЕ загружайте папку `user_files/` (если она пустая)

5. Файлы появятся в списке внизу страницы

---

### ШАГ 3: Создайте коммит

1. Прокрутите страницу вниз
2. Найдите раздел **"Commit changes"**

3. В **верхнем поле** (однострочное) напишите:
   ```
   Первая загрузка бота
   ```
   или
   ```
   Initial commit
   ```

4. **Нижнее поле** (многострочное) можно оставить пустым

5. Нажмите зеленую кнопку **"Commit changes"** внизу

---

### ШАГ 4: Готово! ✅

После нажатия "Commit changes":
- Файлы загрузятся на GitHub
- Появится сообщение "Commit created successfully"
- Вы увидите все ваши файлы в репозитории

---

## 📋 Чек-лист: Что должно быть в репозитории

После загрузки проверьте, что есть:

- [ ] `bot.py` - основной код бота
- [ ] `requirements.txt` - зависимости
- [ ] `README.md` - описание
- [ ] `images/` - папка с изображениями (1.jpg, 2.jpg, 3.jpg, 4.jpg, 5.jpg)
- [ ] `Dockerfile` - для деплоя (если создавали)
- [ ] `bothost.json` - конфигурация (если создавали)
- [ ] `.gitignore` - защита секретов (если есть)

---

## ⚠️ ВАЖНО!

### Что НЕ нужно загружать:

- ❌ `bot_database.db` - база данных (создается автоматически)
- ❌ `.env` - файл с секретами (если есть)
- ❌ `__pycache__/` - временные файлы Python
- ❌ `*.pyc` - скомпилированные файлы

Эти файлы либо создаются автоматически, либо содержат секреты.

---

## 🎨 Визуальная инструкция

```
1. GitHub → Ваш репозиторий
   ↓
2. "Add file" → "Upload files"
   ↓
3. Перетащите файлы из папки "Bayzen Learn"
   ↓
4. Напишите коммит: "Первая загрузка бота"
   ↓
5. "Commit changes"
   ↓
6. Готово! ✅
```

---

## 🔄 Если нужно добавить еще файлы

1. Откройте репозиторий
2. Нажмите **"Add file"** → **"Upload files"**
3. Перетащите новые файлы
4. Напишите коммит (например: "Добавлен Dockerfile")
5. Нажмите **"Commit changes"**

---

## ❓ Частые вопросы

**Q: Можно ли загрузить всю папку сразу?**
A: Да! Выделите все файлы в папке и перетащите в браузер.

**Q: Что делать, если файл не загружается?**
A: Проверьте размер файла. GitHub ограничивает размер до 100 МБ на файл.

**Q: Нужно ли загружать базу данных?**
A: Нет! `bot_database.db` создается автоматически при первом запуске.

**Q: Как загрузить скрытые файлы (например, .gitignore)?**
A: Скрытые файлы тоже можно перетащить. Или создайте их прямо на GitHub через "Add file" → "Create new file".

---

## 🚀 После загрузки

После того как файлы загружены:

1. ✅ Репозиторий готов
2. ✅ Можно подключать к Bothost/Render/другим хостингам
3. ✅ Можно делиться кодом с другими

---

**Удачи с загрузкой! 🎉**

