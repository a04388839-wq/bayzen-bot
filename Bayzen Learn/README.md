# Bayzen Study Bot

Telegram бот для проекта Bayzen с системой управления доступом и обучением.

## Установка

1. Установите Python 3.8 или выше
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Установите переменные окружения:
```bash
# Windows (PowerShell)
$env:BOT_TOKEN="ваш_токен_бота"
$env:ADMIN_USERNAME="Prd_ars"

# Linux/Mac
export BOT_TOKEN="ваш_токен_бота"
export ADMIN_USERNAME="Prd_ars"
```

4. Запустите бота:
```bash
python bot.py
```

## Функционал

### Для администратора:
- Команда `/generate_code` - генерация уникальных кодов доступа
- Админ-панель через `/start` (для администраторов)
- Просмотр статистики, пользователей и заявок на регистрацию

### Для пользователей:
- При первом входе требуется ввести код доступа
- После ввода кода открывается доступ к трем разделам:
  - 📚 Информация о проекте - урок с пагинацией
  - 💰 Расчет дохода - вводная информация
  - 📝 Регистрация - сбор документов (7 этапов)

## База данных

Бот использует SQLite базу данных (`bot_database.db`) для хранения:
- Администраторов
- Кодов доступа
- Состояний пользователей
- Прогресса обучения
- Заявок на регистрацию
